---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
  children:
    ash_servers:  # must be underscore
      hosts:
        ash-1:
          ansible_host: "{{ ash_1_ip }}"
          ansible_user: "{{ ash_1_user }}"
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_hetzner
